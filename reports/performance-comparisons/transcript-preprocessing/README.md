# Transcript Preprocessing Comparisons

This directory stores immutable benchmark evidence for transcript preprocessing
changes. Each run directory contains `metrics.json`; candidate runs may also
contain `quality.json`, `report.md`, and `report.html`.

`latest.md` is the reviewed maintainer summary. Do not publish mock values or
claims of improvement before baseline and candidate artifacts exist.
