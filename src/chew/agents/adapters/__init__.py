"""Future optional agent-runtime adapters belong here.

No graph engine is imported until a separately approved optional runtime is
implemented behind the contracts and ports in sibling packages.
"""
